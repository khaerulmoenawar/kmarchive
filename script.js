const portfolioData = {
    writing: [
        {
            id: "WR-2023-001",
            title: "love is a verb",
            description: "john's said that 'love is a verb', and i agree with that…<br>you know, when we talk about 'love is a verb,' it's like saying that love isn't just this fuzzy feeling that floats around in the air, all dreamy and passive. no, it's more like this dynamic, action-packed thing — a verb, yknow? <br><br>it's not enough to just sit there and say, 'oh, i love you.' love isn't just in the words, it's in the things you do, the choices you make, the way you show up day after day without tiredly doing that constant things. it's those little things you do without even thinking — like holding the door open, or switching positions to walk on the street side.<br><br>it's in the way you support them and having their back, even when their dreams and story sound crazy to everyone else. it's in the way you forgive, even when it's hard, and you work through the tough stuff together. love is a verb because it's about showing, proving, and living it out.<br><br>i mean, think about it — you can't just say you love gaming and expect to become a top-tier gamer overnight. you gotta put in lotta efforts, manage your emotions, tryin harder to level up your skills, stumble a little, and keep going. it's the same with love. it's an ongoing grind, a journey full of highs and lows, but that's what makes it real.<br><br>sometimes it's as simple as leaving a sweet message in the mornin, or as grand as planning a surprise getaway. it's lending a listening ear after a bad day, or holding them close when they're hurting. it's about doing things, big and small, that make the other person's heart swell with warmth and gratitude.<br><br>love is a verb because it's alive — it's in those shared laughs, in wiping away tears, in dancing in the parking lot while being crazy together. it's not static or one-dimensional. it's a living, breathing thing that grows and evolves with time.<br><br>so, yeah, love is more than just a feeling. it's an intentional choice to care, to nurture, to support, and to be there. it's in the doing, the showing up, the being present. love is a verb, and when you embrace it as such, it becomes this beautiful, messy, extraordinary adventure that colors every aspect of your life.",
            date: "Aug 2023",
            category: "Love",
            type: "Song",
            featured: true
        },
        {
            id: "WR-2023-002",
            title: "making a playlist",
            description: "'making a playlist is a delicate art, it's like writing a love letter, but better, in a way. you get to say what you wanna say, without actually saying it. you get to use someone's else poetry to express how you feel'<br><br>umm, well…<br><br>it's like, you've got this canvas of songs, and each one holds a piece of what you're thinking, feeling, wanting to convey. it's like using these melodies to paint emotions, trace memories, and wrap it all up with a musical bow.<br><br>it's a chance to let your heart do the talking, using lyrics as your secret messengers. someone else's words suddenly become your own, and every beat carries a part of you that's a bit hard to put into sentences.<br><br>there's this magic in finding just the right track that captures a moment or a sentiment perfectly. it's like stumbling upon a hidden treasure, a musical gem that resonates deeper than words alone can reach.<br><br>so, you weave these tunes together, like chapters of a book, telling a story that's uniquely yours. it's not just about the songs — it's about the order, the flow, the way one song melts into the next. it's a journey, an experience, and it's all your creation.<br><br>and when you hand someone a playlist you've carefully curated for them, it's like you're giving them a piece of your soul wrapped in melodies. it's a gift that's personal, intimate, and honestly, it's pretty darn magical.<br><br>so yeah, making a playlist — it's like bottling up feelings, memories, and dreams into a symphony of emotions. it's like whispering your heart's desires through a melody, and inviting someone to dance to the rhythm of your thoughts.",
            date: "Sep 2023",
            category: "Love",
            type: "Playlist",
            featured: true
        },
        {
            id: "WR-2023-003",
            title: "becoming the person i don't wanna be",
            description: "sometimes, life can be a curious journey, where at times, you might find yourself becoming someone you never envisioned. it's like strolling down a path that seemed familiar but took an unexpected turn, and suddenly, you're wearing a mask that feels foreign.<br><br>it's those moments when you notice yourself making choices or doing things that don't quite align with the person you aspire to be. it's like trying on different outfits in a store, but none of them quite fits your style. it's normal to feel a bit adrift in those moments, a bit like a puzzle piece that doesn't seem to fit the picture.<br><br>but here's the thing – it's perfectly okay to acknowledge this, to recognize that you might be in a phase of your life where you're not entirely in sync with your true self. life is a journey, after all, with its twists and turns. sometimes, it's like taking the scenic route, and along the way, you accumulate experiences and habits that may not serve you well.<br><br>the beauty in all of this is that it's never too late to recalibrate, to shed those layers that you've picked up along the way, and to rediscover the person you want to be. it's like peeling off those outfits that never quite felt right and stepping back into your own skin. it's about realigning with your values, dreams, and aspirations.<br><br>so, while becoming someone you didn't want to be may be a part of your journey, it can also be the catalyst for rediscovering and embracing the authentic you. it's all a part of this intricate, sometimes messy, but ultimately beautiful process of self-discovery and growth.",
            date: "Sep 2023",
            category: "Life",
            type: "Self-Discovery",
            featured: true
        },
        {
            id: "WR-2023-004",
            title: "even if the mind forgets, the heart will always remember",
            description: "there's this something truly remarkable about memories, especially those tied to love. even when the passage of time has a way of erasing finer details, the heart remains a faithful custodian, eternally guarding the most precious of treasures.<br><br>it's those moments, those fleeting instances when everything seemed to align perfectly in your world. they're like snapshots imprinted on your soul, akin to a tune that keeps playing in your mind. as years go by, these memories may lose their sharpness, like a photograph faded at the edges, but their core, their essence, endures.<br><br>every now and then, you find yourself meandering down memory's winding lane, caught in a daydream of the good old days with her. it's the inside jokes shared over late-night conversations, the secrets whispered under a canopy of stars, and the way her smile could turn even the gloomiest of days into pure sunshine. these moments become imprinted on your heart and etched into your mind, and no matter how hard you try, you can't shake them loose.<br><br>sometimes, she's like a well-worn book, each page filled with stories of your journey together, waiting to be revisited and reread. other times, it's a song that sneaks up on you, transporting you back to a time when you both swayed together under a moonlit sky, the music serenading your love for her.<br><br>even as life carries you forward into new chapters, these memories remain steadfast, like the foundation upon which you build the rest of your life with her. they might not always be at the forefront of your thoughts, but they're there, a warm and familiar presence, like a cozy blanket you can wrap around yourself when you need comfort.<br><br>so, yeah, the mind may play tricks and forget the finer points, but the heart? ah, the heart will always remember. it's the guardian of those moments that held the greatest significance, preserving the love, the joy, and the warmth of those beautiful memories with her.",
            date: "Sep 2023",
            category: "Love",
            type: "Nostalgia",
            featured: true
        },
        {
            id: "WR-2023-005",
            title: "i'm the happiest person alive",
            description: "isn't life a fascinating rollercoaster ride? here i am, at this very moment, and i can honestly say, that i'm the happiest person alive. well, it's not about what others think of me, it's about this incredible, ever-evolving journey i find myself on.<br><br>i've fought lots of battles, the kind that leaves scars on the soul. but i wear those scars as badges of honor because they tell stories of resilience, determination, and the unwavering will to keep moving forward. life has a way of throwing curveballs, and i've learned to swing back with all my might.<br><br>there's something profound in the ability to 'make peace with oneself'. it's a beautiful concept that translates to embracing your true self. picture it like this: you're in a quiet room, facing a mirror, and you see yourself – flaws, quirks, strengths, and all – and you just smile, embracing the person you've become. it's a really powerful feeling.<br><br>but what really tugs at my heartstrings is this overwhelming sense of gratitude. i mean, i'm living the best life i've ever had, and i couldn't be more thankful. it's not about the big, flashy moments, though they've been epic. it's about the everyday, the little things, that seemingly mundane things that weave together to create this beautiful tapestry we call life.",
            date: "Sep 2023",
            category: "Life",
            type: "Positivity",
            featured: true
        },
        {
            id: "WR-2023-006",
            title: "live every minute of it",
            description: "life... life is a wild adventure, and every minute of it is like a precious gem waiting to be discovered. there's this beautiful chaos in simply living, in being fully present in each passing moment.<br><br>it's not about rushing through life, trying to check off boxes on some imaginary list of achievements. no, it's about savoring every second, whether it's filled with laughter or tears, success or setbacks. every minute is a part of our unique story, and it's worth embracing.<br><br>there's magic in the mundane, you see. it's in the way the morning sunlight spills through your window, gently waking you up. it's in the aroma of your favorite perfume, a scent that wraps you in comfort. it's in the sound of rain tapping on the roof, a soothing lullaby for the soul.<br><br>living every minute of it means finding joy in the little things – a heartfelt conversation with a friend, a spontaneous dance in your bedroom, or even a quiet moment of reflection under the starlit sky. it's about being fully engaged with the world around you, noticing the details, and appreciating the beauty in simplicity.",
            date: "Oct 2023",
            category: "Life",
            type: "Life",
            featured: true
        },
        {
            id: "WR-2025-007",
            title: "juara tiga",
            description: "di dunia yang serba cepat dan penuh ambisi, banyak orang berlomba-lomba untuk menjadi nomor satu. menjadi yang paling hebat, paling dikenal, paling diakui. seolah-olah, hanya posisi pertama yang layak dirayakan — dan diingat.<br><br>namun, sering kali kita lupa akan satu hal — semakin tinggi harapan, semakin besar pula kekecewaan. semakin keras usaha mengejar puncak, semakin perih ketika terjatuh.<br><br>padahal hidup tidak selalu tentang menjadi yang terbaik. tidak mengapa jika kita tidak menjadi juara satu. tak masalah bila hanya sampai di posisi kedua. atau bahkan, tak ada salahnya jika kita menjadi juara tiga.<br><br>ya, juara tiga. sebuah posisi yang kerap diremehkan. yang sering kali dipandang sebelah mata, dianggap tak ada harganya. nyatanya, di dalamnya masih ada kata juara. masih ada pencapaian, masih ada perjuangan yang layak dirayakan.<br><br>menjadi juara tiga mengingatkan kita bahwa hidup bukan hanya soal posisi tertinggi. karena yang paling penting: kita tetap bagian dari cerita. kita punya peran — kita tetap berarti.",
            date: "Jun 2025",
            category: "Life",
            type: "Perunggu",
            featured: true
        },
        {
            id: "WR-2025-008",
            title: "(i love) you",
            description: "some people come into our lives like a thunderstorm — loud, exhilarating, and impossible to avoid. they ask for attention, leaving a story of a dramatic skies and rumbling earth in their wake. but you, you were never the storm. you came into my life like the quiet that follows it; the gentle scent of rain on dry soil, a petrichor, a sense of peace that settles over everything, even from afar.<br><br>your love has a way of recalibrating my compass. when life feels complicated and noisy, you are the simple truth i return to. you don't offer grandiose, dramatic solutions, but something far more valuable — a steady perspective and the unwavering belief that we can figure things out together. you aren't just my anchor in the storm — you are the steady compass that guides me home.<br><br>with you, joy isn't just a frantic search for the next big thrill. it's a patient discovery of the wonder hidden in plain sight. it's the comfort of hearing your voice after a long day, closing my eyes and feeling like you're beside me.<br><br>happy birthday and love you, my chippy💚.",
            date: "Jul 2025",
            category: "Love",
            type: "You",
            featured: true
        },
        {
            id: "WR-2025-009",
            title: "a curious journey called 'life'",
            description: "sometimes, life can be a curious journey, where at times, you might find yourself becoming someone you never envisioned. it's like strolling down a path that seemed familiar but took an unexpected turn, and suddenly, you're wearing a mask that feels foreign.<br><br>it's those moments when you notice yourself making choices or doing things that don't quite align with the person you aspire to be. it's like trying on different outfits in a store, but none of them quite fits your style. it's normal to feel a bit adrift in those moments, a bit like a puzzle piece that doesn't seem to fit the picture.<br><br>the beauty in all of this is that it's never too late to recalibrate, to shed those layers that you've picked up along the way, and to rediscover the person you want to be.",
            date: "Aug 2025",
            category: "Life",
            type: "Life",
            featured: true
        },
        {
            id: "WR-2025-010",
            title: "sebuah pena",
            description: "sejak dulu, selalu terpatri dalam pikiran bahwasanya hidup itu ialah sebuah naskah yang sudah ditentukan jalan ceritanya akan kemana dan seperti apa, tanpa kita bisa mengubah cerita tersebut. seakan-akan bahwa kita hanyalah pemeran dalam sebuah cerita yang bernama 'kehidupan'.<br><br>tapi, bagaimana jikalau ternyata itu semua keliru? bagaimana jika ternyata dibalik hal tersebut yang sering disebut dengan 'takdir' itu, kita memiliki peranan yang lebih dari hanya sekadar pemeran? mungkin saja, bukan hanya pada suratan semata kemana kita akan berlabuh, melainkan pada keberanian kita untuk menuliskan kembali babak-babak kehidupan yang belum terungkap.<br><br>maka, mungkin inilah saatnya. saatnya untuk merobek naskah lama yang tak pernah kita tulis, dan memulai lembaran baru dengan penuh keberanian. kita bukan sekadar pion yang bergerak di atas papan, melainkan pemain yang menggerakan setiap pion dengan tujuan.",
            date: "Nov 2025",
            category: "Life",
            type: "Pena",
            featured: true
        },
        {
            id: "WR-2025-011",
            title: "terpaksa dewasa",
            description: "sering kali terlintas dalam kepala pada saat masih kecil 'ingin sekali untuk cepat-cepat menjadi dewasa'. dalam bayangan si kecil itu, ia merasa bahwa jika ia dewasa kelak, ia bisa melakukan banyak hal, tanpa harus mendapatkan larangan dari orang dewasa lainnya.<br><br>seiring waktu berlalu, si kecil yang perlahan mendewasa itu mulai merasakan sedikit demi sedikit akan realita dunia dewasa yang ternyata tak sehangat dan semudah seperti apa yang ia bayang-bayangkan dulu.<br><br>namun meski begitu, satu yang ia sadari, bahwa seberapa berat dan kerasnya hidup, ia harus terus tetap melangkah. dan di antara pahit yang harus ia telan, ia menemukan satu hal yang dulu ia tak pernah sadari, bahwa bertahan pun adalah bentuk keberanian.",
            date: "Nov 2025",
            category: "Life",
            type: "Dewasa",
            featured: true
        }
    ],
    photography: [
        {
            id: "PH-2023-001",
            title: "Building Forms",
            description: "A series exploring architectural minimalism through light and shadow. Each photograph captures the essential forms of brutalist and modernist structures.",
            images: ["BUILDING.png"],
            date: "October 2023",
            category: "Architecture",
            type: "Photographic Series",
            featured: true
        },
        {
            id: "PH-2023-002",
            title: "Night Holes",
            description: "Photographic studies of emptiness and presence. This series explores how negative space defines form, using vast landscapes and intimate interiors.",
            images: ["HOLE.png"],
            date: "August 2023",
            category: "Conceptual",
            type: "Visual Study",
            featured: false
        },
        {
            id: "PH-2023-003",
            title: "Nature's Essence",
            description: "Minimalist portraits that focus on essential human expression. Stripped of color and context, these images reveal the universal language of emotion.",
            images: ["NATURE.png"],
            date: "June 2023",
            category: "Nature",
            type: "Portrait Series",
            featured: true
        },
        {
            id: "PH-2023-004",
            title: "The Grandeur",
            description: "An exploration of pattern and repetition in the built environment. This series finds mathematical beauty in everyday urban scenes.",
            images: ["GRANDE.png"],
            date: "April 2023",
            category: "Urban",
            type: "Documentary Series",
            featured: false
        }
    ],
};

class Portfolio {
    constructor() {
        this.currentItem = null;
        this.allItems = [
            ...portfolioData.writing,
            ...portfolioData.photography,
        ];
        this.audio = new Audio('SPK.mp3');
        this.audio.volume = 0.25;
        this.audio.loop = true;
        this.isAudioPlaying = false;
        this.isDark = true;
        this.mouseX = window.innerWidth / 2;
        this.mouseY = window.innerHeight / 2;
        this.cursorX = this.mouseX;
        this.cursorY = this.mouseY;
        this.isMobile = window.innerWidth <= 768 || !window.matchMedia('(hover: hover)').matches;
        this.isNavOpen = false;

        // Touch state for swipe
        this.touchStartX = 0;
        this.touchStartY = 0;

        this.init();
    }

    init() {
        this.setVhVariable();
        this.setupLoader();
        this.setupCursor();
        this.setupCanvas();
        this.setupClock();
        this.initGalleries();
        this.setupIntersectionObservers();
        this.setupEventListeners();
        this.setupAudio();
        this.setupSorting();
        this.setupScrollHandlers();
        this.setupMobileNav();
        this.setupTouchGestures();
        if (!this.isMobile) this.tickCursor();

        // Handle resize
        let resizeTimer;
        window.addEventListener('resize', () => {
            clearTimeout(resizeTimer);
            resizeTimer = setTimeout(() => {
                this.isMobile = window.innerWidth <= 768 || !window.matchMedia('(hover: hover)').matches;
                this.setVhVariable();
            }, 150);
        });
    }

    // Fix for mobile viewport height (address bar issue)
    setVhVariable() {
        const vh = window.innerHeight * 0.01;
        document.documentElement.style.setProperty('--vh', `${vh}px`);
    }

    setupLoader() {
        const loader  = document.getElementById('loader');
        const bar     = document.getElementById('loaderProgress');
        const count   = document.getElementById('loaderCount');
        const text    = document.getElementById('loaderText');

        const phases = ['INITIALIZING ARCHIVE', 'LOADING ASSETS', 'PREPARING GALLERY', 'READY'];
        let progress = 0;
        let phase = 0;

        const tick = () => {
            progress += Math.random() * 18 + 4;
            if (progress >= 100) {
                progress = 100;
                clearInterval(interval);
                setTimeout(() => {
                    loader.classList.add('hidden');
                    this.startHeroAnimations();
                }, 600);
            }

            bar.style.width = `${progress}%`;
            count.textContent = String(Math.floor(progress)).padStart(3, '0');

            const newPhase = Math.floor((progress / 100) * phases.length);
            if (newPhase !== phase && newPhase < phases.length) {
                phase = newPhase;
                text.textContent = phases[phase];
            }
        };

        const interval = setInterval(tick, 80);
    }

    setupCursor() {
        if (this.isMobile) return;

        document.addEventListener('mousemove', e => {
            this.mouseX = e.clientX;
            this.mouseY = e.clientY;
        });

        document.addEventListener('mousedown', () => {
            document.getElementById('cursor')?.classList.add('clicking');
        });
        document.addEventListener('mouseup', () => {
            document.getElementById('cursor')?.classList.remove('clicking');
        });

        const addHover = (selector, label = '') => {
            document.querySelectorAll(selector).forEach(el => {
                el.addEventListener('mouseenter', () => {
                    const cursor = document.getElementById('cursor');
                    cursor?.classList.add('hovering');
                    if (label) {
                        cursor?.classList.add('gallery-hover');
                        const lbl = document.getElementById('cursorLabel');
                        if (lbl) lbl.textContent = label;
                    }
                });
                el.addEventListener('mouseleave', () => {
                    const cursor = document.getElementById('cursor');
                    cursor?.classList.remove('hovering', 'gallery-hover');
                    const lbl = document.getElementById('cursorLabel');
                    if (lbl) lbl.textContent = '';
                });
            });
        };

        addHover('a, button, .category-btn, .filter-select, .theme-toggle, .back-to-top, .audio-player');
        addHover('.gallery-item:not(.writing-entry)', 'VIEW');
        addHover('.writing-entry', 'READ');
    }

    tickCursor() {
        const cursor = document.getElementById('cursor');
        if (!cursor) return;

        const lerp = (a, b, t) => a + (b - a) * t;
        this.cursorX = lerp(this.cursorX, this.mouseX, 0.1);
        this.cursorY = lerp(this.cursorY, this.mouseY, 0.1);

        cursor.style.transform = `translate(calc(${this.cursorX}px - 50%), calc(${this.cursorY}px - 50%))`;
        requestAnimationFrame(() => this.tickCursor());
    }

    setupCanvas() {
        const canvas = document.getElementById('heroCanvas');
        if (!canvas) return;

        // Skip canvas on mobile for performance
        if (this.isMobile) {
            canvas.style.display = 'none';
            return;
        }

        const ctx = canvas.getContext('2d');
        let W, H, dots = [];
        let animFrame;

        const resize = () => {
            W = canvas.width = canvas.offsetWidth;
            H = canvas.height = canvas.offsetHeight;
            buildDots();
        };

        const buildDots = () => {
            dots = [];
            const spacing = 60;
            const cols = Math.floor(W / spacing);
            const rows = Math.floor(H / spacing);
            for (let c = 0; c <= cols; c++) {
                for (let r = 0; r <= rows; r++) {
                    dots.push({
                        x: (c / cols) * W,
                        y: (r / rows) * H,
                        ox: (c / cols) * W,
                        oy: (r / rows) * H,
                        size: Math.random() * 1.2 + 0.3
                    });
                }
            }
        };

        const draw = () => {
            ctx.clearRect(0, 0, W, H);
            const rect = canvas.getBoundingClientRect();
            const rmx = this.mouseX - rect.left;
            const rmy = this.mouseY - rect.top;

            for (const d of dots) {
                const dx = rmx - d.x;
                const dy = rmy - d.y;
                const dist = Math.sqrt(dx * dx + dy * dy);
                const pull = Math.min(80 / (dist + 1), 6);

                d.x += (d.ox - d.x) * 0.04 + (dist < 200 ? -dx * pull * 0.003 : 0);
                d.y += (d.oy - d.y) * 0.04 + (dist < 200 ? -dy * pull * 0.003 : 0);

                const alpha = dist < 250 ? 0.08 + (1 - dist / 250) * 0.25 : 0.06;
                ctx.beginPath();
                ctx.arc(d.x, d.y, d.size, 0, Math.PI * 2);
                ctx.fillStyle = `rgba(200,184,154,${alpha})`;
                ctx.fill();
            }

            animFrame = requestAnimationFrame(draw);
        };

        // Pause canvas when not visible
        const observer = new IntersectionObserver(entries => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    animFrame = requestAnimationFrame(draw);
                } else {
                    cancelAnimationFrame(animFrame);
                }
            });
        });

        observer.observe(canvas);

        window.addEventListener('resize', resize);
        resize();
        draw();
    }

    setupClock() {
        const el = document.getElementById('headerTime');
        if (!el) return;

        const tick = () => {
            const now = new Date();
            const h = String(now.getHours()).padStart(2, '0');
            const m = String(now.getMinutes()).padStart(2, '0');
            const s = String(now.getSeconds()).padStart(2, '0');
            el.textContent = `${h}:${m}:${s}`;
        };

        tick();
        setInterval(tick, 1000);
    }

    // ── Mobile Navigation ──
    setupMobileNav() {
        // Inject toggle button if not in HTML
        const header = document.getElementById('header');
        if (!header) return;

        let toggle = header.querySelector('.nav-toggle');
        if (!toggle) {
            toggle = document.createElement('button');
            toggle.className = 'nav-toggle';
            toggle.setAttribute('aria-label', 'Toggle navigation');
            toggle.innerHTML = '<span></span><span></span><span></span>';
            header.appendChild(toggle);
        }

        const nav = header.querySelector('.nav');

        toggle.addEventListener('click', () => {
            this.isNavOpen = !this.isNavOpen;
            toggle.classList.toggle('open', this.isNavOpen);
            nav?.classList.toggle('open', this.isNavOpen);
            document.body.classList.toggle('modal-open', this.isNavOpen);
        });

        // Close nav when link is clicked
        document.querySelectorAll('.nav-link').forEach(link => {
            link.addEventListener('click', () => {
                if (this.isNavOpen) {
                    this.isNavOpen = false;
                    toggle.classList.remove('open');
                    nav?.classList.remove('open');
                    document.body.classList.remove('modal-open');
                }
            });
        });
    }

    // ── Touch Swipe for Modal ──
    setupTouchGestures() {
        const modal = document.getElementById('detailModal');
        if (!modal) return;

        modal.addEventListener('touchstart', e => {
            this.touchStartX = e.touches[0].clientX;
            this.touchStartY = e.touches[0].clientY;
        }, { passive: true });

        modal.addEventListener('touchend', e => {
            const dx = e.changedTouches[0].clientX - this.touchStartX;
            const dy = e.changedTouches[0].clientY - this.touchStartY;

            // Only swipe if horizontal movement is dominant
            if (Math.abs(dx) > Math.abs(dy) && Math.abs(dx) > 60) {
                if (dx < 0) this.navigateModal(1);  // swipe left = next
                else        this.navigateModal(-1); // swipe right = prev
            }
        }, { passive: true });
    }

    initGalleries() {
        const writingGrid = document.querySelector('.writing-gallery');
        if (writingGrid) {
            portfolioData.writing.forEach((item, i) => {
                writingGrid.appendChild(this.createWritingEntry(item, i));
            });
        }

        ['photography'].forEach(cat => {
            const grid = document.querySelector(`.${cat}-gallery`);
            if (!grid) return;
            portfolioData[cat].forEach(item => {
                grid.appendChild(this.createCard(item, cat));
            });
        });
    }

    createWritingEntry(item, index) {
        const el = document.createElement('div');
        el.className = 'gallery-item writing-entry';
        el.dataset.category = 'writing';
        el.dataset.id = item.id;

        const num = String(index + 1).padStart(3, '0');

        el.innerHTML = `
            <div class="wr-col-index">
                <span class="wr-num">${num}</span>
                <span class="wr-badge">${item.category}</span>
            </div>
            <div class="wr-col-main">
                <div class="wr-entry-title">${item.title}</div>
                <div class="wr-entry-excerpt">${item.description.replace(/<br>/gi, ' ').replace(/<[^>]+>/g, '')}</div>
                <div class="wr-entry-meta">
                    <span class="wr-meta-date">${item.date}</span>
                    <span class="wr-meta-sep"></span>
                    <span class="wr-meta-type">${item.type}</span>
                </div>
            </div>
            <div class="wr-col-action">
                <div class="wr-arrow" aria-hidden="true">
                    <svg viewBox="0 0 16 16" fill="none" stroke-width="1.5">
                        <path d="M3 13L13 3M13 3H6M13 3v7" stroke="currentColor"/>
                    </svg>
                </div>
                ${item.featured ? `<span class="wr-featured-tag always-show">Featured</span>` : ''}
            </div>
        `;

        el.addEventListener('click', () => this.openModal(item));
        return el;
    }

    createCard(item, category) {
        const el = document.createElement('div');
        el.className = 'gallery-item';
        el.dataset.category = category;
        el.dataset.id = item.id;

        const imgSrc = item.images[0];
        const isLocal = !imgSrc.startsWith('http');
        const fallback = 'https://images.unsplash.com/photo-1486312338219-ce68d2c6f44d?auto=format&fit=crop&w=800&q=80';

        el.innerHTML = `
            <div class="item-image-container">
                <img
                    class="item-image"
                    src="${imgSrc}"
                    alt="${item.title}"
                    loading="lazy"
                    decoding="async"
                    ${isLocal ? `onerror="this.onerror=null;this.src='${fallback}'"` : ''}
                >
                <div class="item-overlay"></div>
                <div class="item-info">
                    <div class="item-category">${item.category || category}</div>
                    <div class="item-title">${item.title}</div>
                    <div class="item-date">${item.date}</div>
                </div>
                <div class="item-view"><i class="fas fa-arrow-up-right-from-square"></i></div>
            </div>
        `;

        el.addEventListener('click', () => this.openModal(item));
        return el;
    }

    setupIntersectionObservers() {
        const sections = document.querySelectorAll('.gallery-section, .hero');
    
    const sectionObs = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (!entry.isIntersecting) return;
            entry.target.classList.add('active');

            const id = entry.target.id;
            document.querySelectorAll('.nav-link').forEach(link => {
                link.classList.toggle('active', link.getAttribute('href') === `#${id}`);
            });
        });
    }, { threshold: 0.2, rootMargin: '-80px' });
    
    sections.forEach(s => {
        if (s.id) sectionObs.observe(s);
    });

    const itemObs = new IntersectionObserver(entries => {
        entries.forEach((entry, i) => {
            if (!entry.isIntersecting) return;
            setTimeout(() => entry.target.classList.add('visible'), i * 70);
            itemObs.unobserve(entry.target);
        });
    }, { threshold: 0.04, rootMargin: '40px' });
    
    document.querySelectorAll('.gallery-item').forEach(el => itemObs.observe(el));
    }
    openModal(item) {
        this.currentItem = item;

        const modal     = document.getElementById('detailModal');
        const modalBody = modal.querySelector('.modal-body');
        const isWriting = portfolioData.writing.includes(item);

        if (isWriting) {
            modalBody.classList.add('writing-modal');
        } else {
            modalBody.classList.remove('writing-modal');
            const modalImage = document.getElementById('modalImage');
            const isLocal = !item.images[0].startsWith('http');
            modalImage.src = item.images[0];
            if (isLocal) {
                modalImage.onerror = () => {
                    modalImage.onerror = null;
                    modalImage.src = 'https://images.unsplash.com/photo-1486312338219-ce68d2c6f44d?auto=format&fit=crop&w=1600&q=80';
                };
            }
            modalImage.alt = item.title;

            const gallery = document.getElementById('modalGallery');
            gallery.innerHTML = '';
            item.images.forEach((src, i) => {
                const thumb = document.createElement('div');
                thumb.className = `modal-gallery-item${i === 0 ? ' active' : ''}`;
                const isL = !src.startsWith('http');
                thumb.innerHTML = `<img src="${src}" alt="${item.title}" loading="lazy" ${isL ? `onerror="this.onerror=null;this.src='https://images.unsplash.com/photo-1486312338219-ce68d2c6f44d?auto=format&fit=crop&w=200&q=60'"` : ''}>`;
                thumb.addEventListener('click', () => {
                    document.getElementById('modalImage').src = src;
                    gallery.querySelectorAll('.modal-gallery-item').forEach(t => t.classList.remove('active'));
                    thumb.classList.add('active');
                });
                gallery.appendChild(thumb);
            });
        }

        document.getElementById('modalTitle').textContent = item.title;
        document.getElementById('modalDescription').innerHTML = item.description;
        document.getElementById('modalCategory').textContent = item.category || '';
        document.getElementById('modalCategory2').textContent = item.category || '';
        document.getElementById('modalDate').textContent = item.date;
        document.getElementById('modalType').textContent = item.type;
        document.getElementById('modalId').textContent = item.id;

        modal.classList.add('active');
        document.body.classList.add('modal-open');

        // Scroll modal to top on open
        setTimeout(() => { modal.scrollTop = 0; }, 10);

        // Focus trap: focus close button
        setTimeout(() => {
            document.getElementById('modalClose')?.focus();
        }, 100);
    }

    closeModal() {
        document.getElementById('detailModal').classList.remove('active');
        document.body.classList.remove('modal-open');
        this.currentItem = null;
    }

    navigateModal(dir) {
        if (!this.currentItem) return;

        let cat = '';
        if (portfolioData.writing.includes(this.currentItem))          cat = 'writing';
        else if (portfolioData.photography.includes(this.currentItem)) cat = 'photography';
        if (!cat) return;

        const items = portfolioData[cat];
        const idx   = items.indexOf(this.currentItem);
        const next  = (idx + dir + items.length) % items.length;

        // Animate out then in
        const content = document.querySelector('.modal-content');
        if (content) {
            content.style.transition = 'transform 0.25s ease, opacity 0.25s';
            content.style.transform = `translateX(${dir > 0 ? '-40px' : '40px'})`;
            content.style.opacity = '0';
            setTimeout(() => {
                this.openModal(items[next]);
                content.style.transform = `translateX(${dir > 0 ? '40px' : '-40px'})`;
                requestAnimationFrame(() => {
                    content.style.transition = 'transform 0.4s var(--ease-expo), opacity 0.4s';
                    content.style.transform = 'translateX(0)';
                    content.style.opacity = '1';
                });
            }, 250);
        } else {
            this.openModal(items[next]);
        }
    }

    setupAudio() {
        const playBtn   = document.getElementById('playBtn');
        const pauseBtn  = document.getElementById('pauseBtn');
        const volumeBtn = document.getElementById('volumeBtn');
        const player    = document.getElementById('audioPlayer');

        playBtn?.addEventListener('click', () => {
            this.audio.play().catch(() => {});
            this.isAudioPlaying = true;
            playBtn.style.display = 'none';
            if (pauseBtn) pauseBtn.style.display = '';
            player?.classList.add('audio-playing');
        });

        pauseBtn?.addEventListener('click', () => {
            this.audio.pause();
            this.isAudioPlaying = false;
            if (pauseBtn) pauseBtn.style.display = 'none';
            playBtn.style.display = '';
            player?.classList.remove('audio-playing');
        });

        volumeBtn?.addEventListener('click', () => {
            if (this.audio.volume > 0) {
                this.audio.volume = 0;
                volumeBtn.innerHTML = '<i class="fas fa-volume-mute"></i>';
            } else {
                this.audio.volume = 0.25;
                volumeBtn.innerHTML = '<i class="fas fa-volume-up"></i>';
            }
        });
    }

    setupSorting() {
        document.getElementById('sortSelect')?.addEventListener('change', e => {
            this.sortAndRedraw(e.target.value);
        });
    }

    sortAndRedraw(criteria) {
        const sorted = [...this.allItems].sort((a, b) => {
            if (criteria === 'date') {
                const da = new Date(a.date.replace(/(\w+)\s(\d{4})/, '$1 1 $2'));
                const db = new Date(b.date.replace(/(\w+)\s(\d{4})/, '$1 1 $2'));
                return db - da;
            }
            if (criteria === 'title')    return a.title.localeCompare(b.title);
            if (criteria === 'featured') return (b.featured ? 1 : 0) - (a.featured ? 1 : 0);
            return 0;
        });

        document.querySelectorAll('.writing-gallery, .photography-gallery').forEach(g => {
            if (g) g.innerHTML = '';
        });

        sorted.forEach((item) => {
            let cat = '';
            if (portfolioData.writing.includes(item))          cat = 'writing';
            else if (portfolioData.photography.includes(item)) cat = 'photography';
            if (!cat) return;

            const grid = document.querySelector(`.${cat}-gallery`);
            if (!grid) return;

            const catIdx = portfolioData[cat].indexOf(item);
            const card = cat === 'writing'
                ? this.createWritingEntry(item, catIdx)
                : this.createCard(item, cat);

            grid.appendChild(card);
            requestAnimationFrame(() => setTimeout(() => card.classList.add('visible'), 60));
        });

        // Re-observe new items
        const itemObs = new IntersectionObserver(entries => {
            entries.forEach((entry, i) => {
                if (!entry.isIntersecting) return;
                const delay = this.isMobile ? 0 : i * 70;
                setTimeout(() => entry.target.classList.add('visible'), delay);
                itemObs.unobserve(entry.target);
            });
        }, { threshold: 0.04, rootMargin: '40px' });
        document.querySelectorAll('.gallery-item:not(.visible)').forEach(el => itemObs.observe(el));
    }

    setupEventListeners() {
        // Category filter buttons
        document.querySelectorAll('.category-btn').forEach(btn => {
            btn.addEventListener('click', () => {
                document.querySelectorAll('.category-btn').forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
                this.filterGalleries(btn.dataset.category);
            });
        });

        // Nav links
        document.querySelectorAll('.nav-link').forEach(link => {
            link.addEventListener('click', e => {
                e.preventDefault();
                const href = link.getAttribute('href');
                const target = document.querySelector(href);

                document.querySelectorAll('.nav-link').forEach(l => l.classList.remove('active'));
                link.classList.add('active');

                if (href === '#home') {
                    window.scrollTo({ top: 0, behavior: 'smooth' });
                    this.filterGalleries('all');
                    document.querySelectorAll('.category-btn').forEach(b => {
                        b.classList.toggle('active', b.dataset.category === 'all');
                    });
                } else if (target) {
                    const cat = href.slice(1);
                    this.filterGalleries(cat === 'writing' || cat === 'photography' ? cat : 'all');
                    document.querySelectorAll('.category-btn').forEach(b => {
                        b.classList.toggle('active', b.dataset.category === (cat === 'about' ? 'all' : cat));
                    });
                    setTimeout(() => target.scrollIntoView({ behavior: 'smooth', block: 'start' }), 50);
                }
            });
        });

        // Modal
        document.getElementById('modalClose')?.addEventListener('click', () => this.closeModal());
        document.getElementById('modalPrev')?.addEventListener('click', () => this.navigateModal(-1));
        document.getElementById('modalNext')?.addEventListener('click', () => this.navigateModal(1));
        document.getElementById('modalBackdrop')?.addEventListener('click', () => this.closeModal());

        // Keyboard
        document.addEventListener('keydown', e => {
            const modal = document.getElementById('detailModal');
            if (!modal?.classList.contains('active')) return;
            if (e.key === 'Escape')     this.closeModal();
            if (e.key === 'ArrowLeft')  this.navigateModal(-1);
            if (e.key === 'ArrowRight') this.navigateModal(1);
        });

        // Theme
        document.getElementById('themeToggle')?.addEventListener('click', () => this.toggleTheme());

        // Back to top
        document.getElementById('backToTop')?.addEventListener('click', e => {
            e.preventDefault();
            window.scrollTo({ top: 0, behavior: 'smooth' });
        });
    }

    filterGalleries(category) {
        document.querySelectorAll('.gallery-section').forEach(section => {
            // Always show about section
            if (section.id === 'about') return;

            const show = category === 'all' || section.id === category;

            if (show) {
                section.style.display = '';
                requestAnimationFrame(() => {
                    section.style.transition = 'opacity 0.5s var(--ease-smooth), transform 0.5s var(--ease-expo)';
                    section.style.opacity = '1';
                    section.style.transform = 'translateY(0)';
                });
            } else {
                section.style.transition = 'opacity 0.4s var(--ease-smooth), transform 0.4s var(--ease-expo)';
                section.style.opacity = '0';
                section.style.transform = 'translateY(30px)';
                setTimeout(() => { section.style.display = 'none'; }, 450);
            }
        });
    }

    toggleTheme() {
        this.isDark = !this.isDark;
        document.body.classList.toggle('light-mode', !this.isDark);
        const icon = document.querySelector('.theme-icon');
        if (icon) icon.className = `fas fa-${this.isDark ? 'moon' : 'sun'} theme-icon`;
    }

    setupScrollHandlers() {
        const header      = document.getElementById('header');
        const headerProg  = document.getElementById('headerProgress');
        const backToTop   = document.getElementById('backToTop');
        const categoryNav = document.getElementById('categoryNav');

        let lastY = window.scrollY;
        let ticking = false;

        window.addEventListener('scroll', () => {
            if (ticking) return;
            requestAnimationFrame(() => {
                const y = window.scrollY;
                const maxScroll = document.body.scrollHeight - window.innerHeight;
                const pct = maxScroll > 0 ? (y / maxScroll) * 100 : 0;

                header?.classList.toggle('scrolled', y > 60);
                if (headerProg) headerProg.style.width = `${pct}%`;
                backToTop?.classList.toggle('visible', y > 600);

                if (categoryNav) {
                    const pastHero = y > window.innerHeight * 0.7;
                    const scrollingDown = y > lastY + 5; // add threshold to avoid jitter
                    categoryNav.classList.toggle('hidden', pastHero && scrollingDown);
                }

                lastY = y;
                ticking = false;
            });
            ticking = true;
        }, { passive: true });
    }

    startHeroAnimations() {
        document.querySelectorAll('.hero-word').forEach((word, i) => {
            setTimeout(() => {
                word.style.transition = `transform 1.4s cubic-bezier(0.19,1,0.22,1), opacity 1s`;
                word.style.transform = 'translateY(0)';
                word.style.opacity = '1';
            }, i * 250 + 100);
        });

        const eyebrow = document.querySelector('.hero-eyebrow');
        if (eyebrow) setTimeout(() => {
            eyebrow.style.transition = 'opacity 0.8s, transform 0.8s var(--ease-expo)';
            eyebrow.style.opacity = '1';
            eyebrow.style.transform = 'translateY(0)';
        }, 200);

        const subtitle = document.querySelector('.hero-subtitle');
        if (subtitle) setTimeout(() => {
            subtitle.style.transition = 'opacity 1s 0.1s, transform 1s var(--ease-expo)';
            subtitle.style.opacity = '1';
            subtitle.style.transform = 'translateY(0)';
        }, 900);

        const cta = document.querySelector('.hero-cta');
        if (cta) setTimeout(() => {
            cta.style.transition = 'opacity 0.8s, transform 0.8s var(--ease-expo)';
            cta.style.opacity = '1';
            cta.style.transform = 'translateY(0)';
        }, 1100);

        const scroll = document.querySelector('.hero-scroll-indicator');
        if (scroll) setTimeout(() => {
            scroll.style.transition = 'opacity 1s';
            scroll.style.opacity = '1';
        }, 1600);

        const index = document.querySelector('.hero-index');
        if (index) setTimeout(() => {
            index.style.transition = 'opacity 1s, transform 1s var(--ease-expo)';
            index.style.opacity = '1';
            index.style.transform = 'translateX(0)';
        }, 1400);
    }
}

document.addEventListener('DOMContentLoaded', () => {
    new Portfolio();
});
